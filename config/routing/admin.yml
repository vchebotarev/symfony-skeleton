
# prefix: /admin

app_admin_default_index:
    path:     /
    defaults: { _controller: App\Controller\Admin\DefaultController:indexAction }

# Profile
app_admin_profile:
    prefix:   /profile
    resource: 'admin/profile.yml'

# Users
app_admin_user:
    prefix:   /user
    resource: 'admin/user.yml'

# Auth log
app_admin_auth_log:
    prefix:   /auth-log
    resource: 'admin/auth_log.yml'
