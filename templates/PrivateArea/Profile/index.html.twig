
{% extends 'layout/main.html.twig' %}

{% block page_title %}
    Профиль
{% endblock %}

{% block content %}

    <a href="{{ path('app_private_profile_change_username') }}" class="btn btn-primary">Изменить логин</a>
    <br>
    <br>
    <a href="{{ path('app_private_profile_change_password') }}" class="btn btn-primary">Изменить пароль</a>
    <br>
    <br>
    <a href="{{ path('app_private_profile_change_timezone') }}" class="btn btn-primary">Изменить часовой пояс</a>
    {% if is_granted('user.change.email') %}
        <br>
        <br>
        <a href="{{ path('app_private_profile_change_email') }}" class="btn btn-primary">Изменить e-mail</a>
    {% endif %}

{% endblock %}